name: myfirst workflow
on: workflow_dispatch
jobs:
  myfirst_task:
  runs-on: ubuntu-latest
  steps:
    - name: print greeting
      run: echo "hello world"
    - name: print goodbye
      run: echo "done -bye!"
